/**
 * Login Page - Modern Premium UI
 */

import { LoginForm } from '@/src/components/LoginFormModern';

export const metadata = {
  title: 'Login - Food Share',
  description: 'Login to your Food Share account',
};

export default function LoginPage() {
  return <LoginForm />;
}
